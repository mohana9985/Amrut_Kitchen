/*
 * Amrut Kitchen — Config Template
 * =================================
 * Copy this file to config.js and fill in real values.
 * config.js is gitignored — never commit real URLs to GitHub.
 */

const AMRUT_CONFIG = {
  // Paste your Google Sheets CSV URL here after publishing
  sheetUrl: 'REPLACE_WITH_YOUR_GOOGLE_SHEETS_CSV_URL'
};
